# Learnings — verify

> **Per-skill, cross-project memory** — lessons about browser verification that apply across every project
>
> **When to append:** after any verify run where you find a pitfall, shortcut, or project-agnostic lesson
> **When to read:** every Pre-flight — grep keyword of current task → apply before starting
>
> **Pruning:** stale entries (framework changed, MCP API changed) → mark `~~deprecated~~`

---

## Entries

<!-- newest on top -->
